[pytest]
testpaths = tests api_tests
addopts = --html=reports/report.html --self-contained-html -v
markers =
    smoke: fast, critical-path checks safe to run on every commit
    regression: broader coverage run before releases
    critical_path: highest business-impact user flows (login, checkout)
    api: tests that hit an API directly instead of driving the UI
